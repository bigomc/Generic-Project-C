#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include "other.h"

void fileCheck(const char *fileName);

int main (void) {

	/*verifyDeviceTree();

	if(pciDevices()) {
		printf("Valid NIC\n");;		
	} else {
		printf("Didn't find a valid NIC\n");
	}*/

	publicFunction();

	return 0;
}